from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Machine learning models for predicting drug-target interactions (DTIs) and other properties.',
    author='Ricardo Avila',
    license='MIT',
)
